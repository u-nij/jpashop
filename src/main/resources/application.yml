spring:
    datasource:
        url: jdbc:h2:tcp://localhost/~/jpashop
        username: sa
        password:
        driver-class-name: org.h2.Driver

    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                # show_sql: true  # 운영환경에서 쓰지 않음
                format_sql: true
logging.level:
    org.hibernate.SQL: debug
    # org.hibernate.type: trace